from rich.console import Console

console = Console()


def run(directory: str, model_override: str | None, auto_accept: bool) -> None:
    console.print("[yellow]evalfix fix[/yellow] — not yet implemented")
