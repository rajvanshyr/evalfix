from rich.console import Console

console = Console()


def run(directory: str, model_override: str | None, as_json: bool) -> None:
    console.print("[yellow]evalfix run[/yellow] — not yet implemented")
